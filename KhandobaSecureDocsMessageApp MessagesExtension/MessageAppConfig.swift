import Foundation

enum MessageAppConfig {
    // Must match your iCloud container entitlement in both targets
    static let cloudKitContainer = "iCloud.com.khandoba.securedocs"
    
    // Must match your App Group in both targets
    static let appGroupIdentifier = "group.com.khandoba.securedocs"
}
