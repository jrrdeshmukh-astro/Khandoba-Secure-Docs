<Page x:Class="KhandobaSecureDocs.Views.AntiVaultDetailView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer>
        <StackPanel Margin="20" Spacing="16">
            <!-- Status Card -->
            <Border Background="LightGray" Padding="16" CornerRadius="8">
                <StackPanel Spacing="8">
                    <TextBlock Text="Status" FontSize="18" FontWeight="Bold"/>
                    <TextBlock x:Name="StatusText" FontSize="16"/>
                    <TextBlock x:Name="LastUnlockedText" FontSize="12" Foreground="Gray" Visibility="Collapsed"/>
                </StackPanel>
            </Border>

            <!-- Monitored Vault Card -->
            <Border Background="LightGray" Padding="16" CornerRadius="8">
                <StackPanel Spacing="8">
                    <TextBlock Text="Monitored Vault" FontSize="18" FontWeight="Bold"/>
                    <TextBlock x:Name="MonitoredVaultText" FontSize="14" Foreground="Gray"/>
                </StackPanel>
            </Border>

            <!-- Auto-Unlock Policy Card -->
            <Border Background="LightGray" Padding="16" CornerRadius="8">
                <StackPanel Spacing="12">
                    <TextBlock Text="Auto-Unlock Policy" FontSize="18" FontWeight="Bold"/>
                    <CheckBox x:Name="UnlockOnSessionCheck" Content="Unlock on Session Nomination" IsEnabled="False"/>
                    <CheckBox x:Name="UnlockOnSubsetCheck" Content="Unlock on Subset Nomination" IsEnabled="False"/>
                    <CheckBox x:Name="RequireApprovalCheck" Content="Require Approval" IsEnabled="False"/>
                </StackPanel>
            </Border>

            <!-- Threat Detection Settings Card -->
            <Border Background="LightGray" Padding="16" CornerRadius="8">
                <StackPanel Spacing="12">
                    <TextBlock Text="Threat Detection Settings" FontSize="18" FontWeight="Bold"/>
                    <CheckBox x:Name="DetectContentCheck" Content="Detect Content Discrepancies" IsEnabled="False"/>
                    <CheckBox x:Name="DetectMetadataCheck" Content="Detect Metadata Mismatches" IsEnabled="False"/>
                    <CheckBox x:Name="DetectAccessPatternCheck" Content="Detect Access Pattern Anomalies" IsEnabled="False"/>
                    <CheckBox x:Name="DetectGeographicCheck" Content="Detect Geographic Inconsistencies" IsEnabled="False"/>
                    <CheckBox x:Name="DetectEditHistoryCheck" Content="Detect Edit History Discrepancies" IsEnabled="False"/>
                    <TextBlock x:Name="MinThreatSeverityText" FontSize="14" Foreground="Gray" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- Detected Threats Card -->
            <Border x:Name="ThreatsCard" Background="LightGray" Padding="16" CornerRadius="8" Visibility="Collapsed">
                <StackPanel Spacing="12">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <SymbolIcon Symbol="Warning" FontSize="20" Foreground="Red"/>
                        <TextBlock Text="Detected Threats" FontSize="18" FontWeight="Bold"/>
                        <Button Content="View All" Click="OnViewAllThreatsClick" HorizontalAlignment="Right"/>
                    </StackPanel>
                    <ListView x:Name="ThreatsListView" MaxHeight="200">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Spacing="4">
                                    <TextBlock Text="{Binding Type}" FontSize="14" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="Gray" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Severity}" FontSize="10" Foreground="Red"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Actions -->
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button x:Name="UnlockButton" 
                        Content="Unlock Anti-Vault" 
                        Click="OnUnlockClick"
                        Visibility="Collapsed"
                        Padding="15,8"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
