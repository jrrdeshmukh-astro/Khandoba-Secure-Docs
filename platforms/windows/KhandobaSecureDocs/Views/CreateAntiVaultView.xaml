<Page x:Class="KhandobaSecureDocs.Views.CreateAntiVaultView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer>
        <StackPanel Margin="20" Spacing="16" MaxWidth="600">
            <TextBlock Text="Create Anti-Vault" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- Vault Selection -->
            <Border Background="LightGray" Padding="16" CornerRadius="8">
                <StackPanel Spacing="12">
                    <TextBlock Text="Select Vault to Monitor" FontSize="18" FontWeight="Bold"/>
                    <ComboBox x:Name="VaultComboBox" 
                              DisplayMemberPath="Name"
                              SelectionChanged="OnVaultSelectionChanged"/>
                </StackPanel>
            </Border>

            <!-- Auto-Unlock Policy -->
            <Border Background="LightGray" Padding="16" CornerRadius="8">
                <StackPanel Spacing="12">
                    <TextBlock Text="Auto-Unlock Policy" FontSize="18" FontWeight="Bold"/>
                    <CheckBox x:Name="UnlockOnSessionCheck" Content="Unlock on Session Nomination"/>
                    <CheckBox x:Name="UnlockOnSubsetCheck" Content="Unlock on Subset Nomination"/>
                    <CheckBox x:Name="RequireApprovalCheck" Content="Require Approval"/>
                </StackPanel>
            </Border>

            <!-- Threat Detection Settings -->
            <Border Background="LightGray" Padding="16" CornerRadius="8">
                <StackPanel Spacing="12">
                    <TextBlock Text="Threat Detection Settings" FontSize="18" FontWeight="Bold"/>
                    <CheckBox x:Name="DetectContentCheck" Content="Detect Content Discrepancies" IsChecked="True"/>
                    <CheckBox x:Name="DetectMetadataCheck" Content="Detect Metadata Mismatches" IsChecked="True"/>
                    <CheckBox x:Name="DetectAccessPatternCheck" Content="Detect Access Pattern Anomalies" IsChecked="True"/>
                    <CheckBox x:Name="DetectGeographicCheck" Content="Detect Geographic Inconsistencies" IsChecked="True"/>
                    <CheckBox x:Name="DetectEditHistoryCheck" Content="Detect Edit History Discrepancies" IsChecked="True"/>
                    
                    <TextBlock Text="Minimum Threat Severity" FontSize="14" Margin="0,8,0,0"/>
                    <ComboBox x:Name="SeverityComboBox" SelectedIndex="1">
                        <ComboBoxItem Content="Low"/>
                        <ComboBoxItem Content="Medium"/>
                        <ComboBoxItem Content="High"/>
                        <ComboBoxItem Content="Critical"/>
                    </ComboBox>
                </StackPanel>
            </Border>

            <!-- Error Message -->
            <TextBlock x:Name="ErrorMessageText" 
                       Foreground="Red" 
                       Visibility="Collapsed"
                       TextWrapping="Wrap"/>

            <!-- Actions -->
            <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
                <Button Content="Cancel" 
                        Click="OnCancelClick"
                        Padding="15,8"/>
                <Button Content="Create" 
                        x:Name="CreateButton"
                        Click="OnCreateClick"
                        Padding="15,8"
                        IsEnabled="False"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
