<Page x:Class="KhandobaSecureDocs.Views.AcceptTransferView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Accept Ownership Transfer" 
                       FontSize="28" 
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>
        </StackPanel>

        <!-- Info Card -->
        <Border Grid.Row="1" 
                BorderBrush="LightBlue" 
                BorderThickness="1"
                Background="LightBlue"
                CornerRadius="5"
                Padding="15"
                Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <SymbolIcon Symbol="Info" 
                            FontSize="24" 
                            Foreground="Blue"/>
                <TextBlock Text="Enter the transfer token provided by the vault owner to accept ownership."
                           FontSize="12"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Transfer Token Input -->
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="Transfer Token *" 
                       FontSize="16" 
                       FontWeight="SemiBold"
                       Margin="0,0,0,10"/>
            <TextBox x:Name="TransferTokenTextBox"
                     PlaceholderText="Enter transfer token"
                     FontSize="16"
                     TextChanged="OnTokenTextChanged"/>
        </StackPanel>

        <!-- Accept Button -->
        <Button Grid.Row="4"
                Content="Accept Ownership" 
                x:Name="AcceptButton"
                Click="OnAcceptClick"
                IsEnabled="False"
                Padding="20,10"/>

        <!-- Progress Ring -->
        <ProgressRing Grid.Row="0" 
                      Grid.RowSpan="5"
                      x:Name="LoadingRing"
                      IsActive="False"
                      Width="50" 
                      Height="50"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
    </Grid>
</Page>
