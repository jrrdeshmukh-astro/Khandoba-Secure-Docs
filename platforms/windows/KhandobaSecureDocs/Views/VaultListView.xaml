<Page x:Class="KhandobaSecureDocs.Views.VaultListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="using:KhandobaSecureDocs.ViewModels">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20">
            <TextBlock Text="My Vaults" FontSize="32" FontWeight="Bold"/>
            <Button Content="+ New Vault" 
                    Click="OnCreateVaultClick"
                    Margin="20,0,0,0"
                    HorizontalAlignment="Right"/>
        </StackPanel>

        <!-- Broadcast Vaults Section -->
        <StackPanel Grid.Row="1" x:Name="BroadcastVaultsPanel" Margin="20,10,20,20" Visibility="Collapsed">
            <TextBlock Text="Public Vaults" 
                       FontSize="18" 
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>
            <ListView x:Name="BroadcastVaultsListView"
                      ItemsSource="{x:Bind BroadcastVaults, Mode=OneWay}"
                      SelectionMode="Single"
                      ItemClick="OnVaultClick">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Broadcast Icon -->
                            <SymbolIcon Symbol="Public" 
                                        Grid.Column="0"
                                        FontSize="24"
                                        Foreground="Blue"
                                        Margin="0,0,15,0"/>
                            
                            <!-- Vault Info -->
                            <StackPanel Grid.Column="1">
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <TextBlock Text="{Binding Name}" 
                                               FontSize="18" 
                                               FontWeight="SemiBold"/>
                                    <Border Background="LightBlue" 
                                            Padding="8,4"
                                            CornerRadius="3">
                                        <TextBlock Text="Public" 
                                                   FontSize="12" 
                                                   FontWeight="SemiBold"/>
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="{Binding Status}" 
                                           FontSize="14" 
                                           Foreground="Gray"/>
                            </StackPanel>
                            
                            <!-- Arrow -->
                            <SymbolIcon Symbol="ChevronRight" 
                                        Grid.Column="3"
                                        Foreground="Gray"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <!-- User Vaults Section -->
        <StackPanel Grid.Row="2" Margin="20,0,20,10" x:Name="UserVaultsPanel">
            <TextBlock Text="Your Vaults" 
                       FontSize="18" 
                       FontWeight="Bold"
                       Margin="0,0,0,10"
                       Visibility="{x:Bind HasBroadcastVaults, Mode=OneWay}"/>
            <ListView x:Name="UserVaultsListView"
                      ItemsSource="{x:Bind ViewModel.Vaults, Mode=OneWay}"
                      SelectionMode="Single"
                      ItemClick="OnVaultClick">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Icon -->
                            <SymbolIcon Symbol="Lock" 
                                        Grid.Column="0"
                                        FontSize="24"
                                        Margin="0,0,15,0"/>
                            
                            <!-- Vault Info -->
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Name}" 
                                           FontSize="18" 
                                           FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Status}" 
                                           FontSize="14" 
                                           Foreground="Gray"/>
                            </StackPanel>
                            
                            <!-- Arrow -->
                            <SymbolIcon Symbol="ChevronRight" 
                                        Grid.Column="2"
                                        Foreground="Gray"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <!-- Loading Indicator -->
        <ProgressRing Grid.Row="2" 
                      IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                      Width="50" 
                      Height="50"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
    </Grid>
</Page>
